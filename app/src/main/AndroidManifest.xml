<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rainyteam.controller">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".PasswordRecoveryActivity"></activity>
        <activity android:name="com.rainyteam.controller.SigninActivity"></activity>
        <activity android:name="com.rainyteam.controller.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.rainyteam.controller.EncyclopediaActivity"></activity>
        <activity android:name="com.rainyteam.controller.GreenhouseActivity"></activity>
        <activity android:name="com.rainyteam.controller.StoreActivity"></activity>
    </application>

</manifest>